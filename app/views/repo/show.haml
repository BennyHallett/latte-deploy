%ol.breadcrumb
  %li
    %a{ href: root_path }
      Latte Deploy
  %li
    %a{ href: show_project_path(project: @repo.project.key) }
      = @repo.project.name
  %li
    = @repo.name

.repo-header
  %h2
    = @repo.name
  .location
    %i.fa.fa-globe
    &nbsp;
    = @repo.uri
